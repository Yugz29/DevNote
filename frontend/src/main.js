import './styles/main.css'

// Point d'entrée de l'application
console.log('🚀 DevNote frontend started')

// Router simple (on améliorera plus tard)
const app = document.getElementById('app')
app.innerHTML = `
  <div class="container">
    <h1>DevNote</h1>
    <p>Frontend en construction...</p>
  </div>
`
